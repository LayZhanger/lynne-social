# wheel/browser 测试结果

## 当前状态

20 browser 测试全部通过。CTest 注册 20 个 test case，与其他模块合计运行约 69 秒。

## 文件清单

### UT: BrowserConfig

| 二进制 | 源 | 数量 | 状态 |
|--------|-----|------|------|
| `test_browser_models_ut` | `test_browser_models_ut.cpp` | 6 tests | ✅ 全 PASS |

### TA: CdpBrowserManager 生命周期

| 二进制 | 源 | suites | 状态 |
|--------|-----|--------|------|
| `test_browser_ta` | `test_browser_ta.cpp` | 10 suites, 24 checks | ✅ 全 PASS |

### TA: 全部接口验证（百度真实站）

| 二进制 | 源 | suites | 状态 |
|--------|-----|--------|------|
| `test_browser_baidu_ta` | `test_browser_baidu_ta.cpp` | 11 suites, 44 checks | ✅ 全 PASS |

### TA: 人机交互 7 方法

| 二进制 | 源 | suites | 状态 |
|--------|-----|--------|------|
| `test_browser_human_ta` | `test_browser_human_ta.cpp` | 9 suites, 15 checks | ✅ 全 PASS |

### TA: 边界/错误路径

| 二进制 | 源 | suites | 状态 |
|--------|-----|--------|------|
| `test_browser_human_ta_edge` | `test_browser_human_ta_edge.cpp` | 9 suites, 17 checks | ✅ 全 PASS |

### TA: 小红书跨站验证

| 二进制 | 源 | suites | 状态 |
|--------|-----|--------|------|
| `test_browser_xiaohongshu_ta` | `test_browser_xiaohongshu_ta.cpp` | 7 suites | ✅ 全 PASS |

## 历史修复记录

| 问题 | 修复版本 | 方案 |
|------|----------|------|
| CDP `Input.dispatchMouseEvent` 不触发 `<input type=submit>` 表单提交 | `bda7915` | click 改用 JS `form.submit()` + `location.href` |
| CDP `Input.insertText` 输入框无焦点 | `bda7915` | type 改用 JS `el.focus()` + `el.value` + `dispatchEvent` |
| CSS selector 含单引号时 JS 字符串截断 | `bda7915` | 5 个方法加 `js_selector()` escaping |
| navigate 后立即检查元素找不到 | `bda7915` | navigate 回调内加 `wait_for_selector` |
| 百度搜索结果页 JS 动态渲染 | `bda7915` | TA 改用动态创建测试元素，不依赖搜索 DOM |
| SVG 元素 `className` 是 `SVGAnimatedString` 而非 String | `43167bd` | `typeof` guard + `String()` 包裹 + 平面扫描 |
| 递归 JS 在原始字符串中 `)JS"` 闭合冲突 | `43167bd` | 弃 IIFE，固定分隔符 `JSON_END` |
| 小红书截屏后 CDP 回调 use-after-free | `43167bd` | 截图超时 skip，不析构 pending |
