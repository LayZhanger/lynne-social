# wheel/llm 测试结果

## 1. 汇总

| 总数 | 通过 | 失败 | 覆盖率 |
|------|------|------|--------|
| 19 | 19 | 0 | 100% |

> 覆盖率 100% — 无未覆盖行。因为 impl 使用 stdlib `urllib.request`，无需测同步 client 分支。

## 2. 用例明细

### UT: LLMConfig

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_defaults` | `LLMConfig()` | provider="deepseek", model="deepseek-chat", temperature=0.7, max_tokens=4096, timeout=60 | ✅ |
| `test_custom_values` | `LLMConfig(provider="openai", api_key="sk-123", ...)` | 所有字段可自定义 | ✅ |
| `test_equality` | `LLMConfig() == LLMConfig()` | True | ✅ |
| `test_inequality` | `LLMConfig(api_key="a") == LLMConfig(api_key="b")` | False | ✅ |

### UT: 工厂

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_create_with_config` | `LLMConfig(api_key="sk-test")` | instanceof DeepSeekEngine, name="llm(deepseek:deepseek-chat)" | ✅ |
| `test_create_non_config_uses_default` | `LLMConfig(api_key="sk-test")` | instanceof DeepSeekEngine | ✅ |
| `test_create_missing_api_key_raises` | `LLMConfig(api_key="")` | ValueError("api_key") | ✅ |
| `test_create_with_custom_model` | `LLMConfig(api_key="sk-test", model="deepseek-reasoner")` | name 含 "deepseek-reasoner" | ✅ |

### UT: DeepSeekEngine

#### 生命周期

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_start_stop` | start → stop | name 正确，不抛异常 | ✅ |
| `test_health_check_no_network` | 无网络 | False | ✅ |

#### chat 正常流程

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_chat_plain` | `[{"role":"user","content":"hi"}]` | `{"role":"assistant","content":"Hello, how can I help?"}` | ✅ |
| `test_chat_with_tool_calls` | messages + tools | tool_calls[0].function.name="search", arguments='{"keyword":"AI"}' | ✅ |
| `test_chat_includes_tools_in_request` | chat(msgs, tools=[...]) | 请求体中 tools 字段等于传入值 | ✅ |
| `test_custom_base_url` | base_url="https://custom.llm/api/v1" | 请求 URL 以自定义地址开头 | ✅ |
| `test_custom_temperature_and_max_tokens` | temperature=0.3, max_tokens=512 | 请求体中 temperature=0.3, max_tokens=512 | ✅ |

#### 错误处理

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_chat_http_error` | mock 返回 HTTP 401 | RuntimeError("LLM API returned 401: ...") | ✅ |
| `test_chat_http_error_with_unreadable_body` | mock 返回 HTTP 500，fp.read() 抛 OSError | RuntimeError("LLM API returned 500: ") | ✅ |
| `test_chat_urllib_error` | mock 抛 URLError("connection refused") | RuntimeError("LLM API unreachable: connection refused") | ✅ |

#### 名称

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_name_reflects_provider_and_model` | model="deepseek-reasoner" | "llm(deepseek:deepseek-reasoner)" | ✅ |

## 3. 未覆盖说明

| 场景 | 原因 |
|------|------|
| `health_check` 成功路径 | 需要真实 DeepSeek API key，留到集成测试 |
| 真实 API 调用 | 同上，留到 E2E |
