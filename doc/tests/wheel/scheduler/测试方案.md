# wheel/scheduler 测试方案

## 1. 被测目标

| 源文件 | 类型 | 测试文件 |
|--------|------|---------|
| `scheduler_models.h` — `SchedulerConfig` | UT | `tests/wheel/scheduler/test_scheduler_ut.cpp` |
| `scheduler_factory.h` — `SchedulerFactory` | UT | 同上 |
| `imp/uv_scheduler.h/.cpp` — `UvScheduler` | TA | `tests/wheel/scheduler/test_scheduler_ta.cpp` |

## 2. 测试点清单

### UT: SchedulerConfig
- [x] struct 默认值
- [x] from_json 解析

### UT: Factory
- [x] create() — 返回 UvScheduler

### TA: UvScheduler 全链路
- [x] 生命周期 — name/start/stop/health
- [x] run_blocking — 线程池执行
- [x] post — 跨线程回调
- [x] add_job — 一次性/重复定时器
- [x] remove_job — 停止并清理
- [x] 多 job 独立
- [x] 异常安全 — 用户回调不崩溃 scheduler
- [x] 析构安全 — stop 后清理

## 3. 运行命令

```bash
dist/bin/test_scheduler_ut
dist/bin/test_scheduler_ta
```
