# wheel/config 测试结果

## 1. 汇总

| 总数 | 通过 | 失败 |
|------|------|------|
| 17 (UT) + 34 (TA) | 51 | 0 |

## 2. 用例明细

### UT: 数据模型 (17 例)
- [x] 6 个 struct 全部覆盖 — 默认值、from_json、枚举
### TA: JsonConfigLoader (34 例)
- [x] 文件 IO 全链路 — missing/default/full/reload/env

## 3. 未覆盖说明

- `config` property — `init` 中已调用 `load()`，不可达
