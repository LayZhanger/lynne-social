# wheel/config 测试结果

## 1. 汇总

| 总数 | 通过 | 失败 | 覆盖率 |
|------|------|------|--------|
| 39 | 39 | 0 | 98% |

源文件覆盖率明细：

| 源文件 | 覆盖率 |
|--------|--------|
| `config_models.py` | 100% |
| `config_factory.py` | 100% |
| `config_loader.py` | 100% |
| `imp/yaml_config_loader.py` | 98% (45/46) |

## 2. 用例明细

### UT: 数据模型

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_defaults` (ServerConfig) | `ServerConfig()` | port=7890, auto_open_browser=True | ✅ |
| `test_custom` (ServerConfig) | `ServerConfig(port=3000, auto_open_browser=False)` | 值匹配 | ✅ |
| `test_defaults` (LLMConfig) | `LLMConfig()` | provider=deepseek, 全部默认 | ✅ |
| `test_api_key_assignment` | `LLMConfig(api_key="sk-xxx")` | api_key="sk-xxx" | ✅ |
| `test_provider_custom` | `LLMConfig(provider="openai", base_url="...")` | provider=openai | ✅ |
| `test_defaults` (BrowserConfig) | `BrowserConfig()` | headless=False, slow_mo=500 | ✅ |
| `test_headless` | `BrowserConfig(headless=True)` | headless=True | ✅ |
| `test_defaults` (PlatformConfig) | `PlatformConfig()` | enabled=False | ✅ |
| `test_requires_name` | `TaskConfig()` | ValidationError | ✅ |
| `test_defaults_with_name` | `TaskConfig(name="test_task")` | 全默认, name 正确 | ✅ |
| `test_full` (TaskConfig) | 全字段赋值 | 逐字段匹配 | ✅ |
| `test_empty_config` | `Config()` | 全部子模块默认值 | ✅ |
| `test_validate_from_dict` | `{server: {port: 9999}, llm: {provider: "openai"}}` | 嵌套解析正确 | ✅ |
| `test_validate_from_full_dict` | 完整 config dict | 全层级字段正确 | ✅ |
| `test_json_serialization` | `Config(server=ServerConfig(port=5555))` | model_dump 后 port=5555 | ✅ |
| `test_platforms_default_type` | `cfg.platforms["twitter"] = ...` | dict 操作正常 | ✅ |
| `test_tasks_default_type` | `cfg.tasks.append(...)` | list 操作正常 | ✅ |

### UT: 工厂

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_create_default` | `ConfigLoaderFactory().create(None)` | instanceof YamlConfigLoader | ✅ |
| `test_create_with_path` | `create("/tmp/custom.yaml")` | instanceof YamlConfigLoader | ✅ |
| `test_create_returns_config_loader` | `create(None)` | instanceof ConfigLoader | ✅ |

### UT: env 解析

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_resolve_env_value` | `"prefix_${KEY}_suffix"`, KEY=test_value | "prefix_test_value_suffix" | ✅ |
| `test_resolve_env_missing_var` | `${MISSING_VAR}`, 未设置 | ValueError | ✅ |
| `test_resolve_env_no_placeholder` | "plain_text" | "plain_text" | ✅ |
| `test_resolve_env_recursive_dict` | `{"a": "${KEY}", "b": {"c": "${KEY}"}}` | 嵌套替换 | ✅ |
| `test_resolve_env_recursive_list` | `["${KEY}", "plain"]` | ["val", "plain"] | ✅ |
| `test_resolve_env_recursive_non_string` | 42 | 42 原样 | ✅ |

### TA: YamlConfigLoader 全链路

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_load_missing_file` | 不存在的文件路径 | Config() 默认值 | ✅ |
| `test_load_yaml_structure` | 完整 sample_yaml | 各字段解析正确 | ✅ |
| `test_load_platforms` | sample_yaml | platforms["twitter"].enabled=True | ✅ |
| `test_load_tasks` | sample_yaml | tasks[0].name="AI动态" | ✅ |
| `test_reload` | 修改文件内容后 reload | 新值生效 | ✅ |
| `test_config_property_after_load` | load 后 .config | 返回 Config 不报错 | ✅ |
| `test_env_var_substitution` | `${API_KEY}` 已设置 | 替换为真实值 | ✅ |
| `test_env_var_in_nested_dict` | `${TOKEN}` 在嵌套 dict | 替换为真实值 | ✅ |
| `test_env_var_in_list` | `${KW}` 在 list | 替换为真实值 | ✅ |
| `test_config_property_raises_before_load` | load 后访问 .config | 不抛异常 | ✅ |
| `test_empty_yaml_file` | 空文件 | Config() 默认 | ✅ |
| `test_none_yaml_file` | `null` 文件 | Config() 默认 | ✅ |

## 3. 未覆盖说明

| 文件:行号 | 原因 |
|-----------|------|
| `yaml_config_loader.py:61` | `config` property 的 `RuntimeError` 分支 — `__init__` 中已调用 `load()`，不可达 |
