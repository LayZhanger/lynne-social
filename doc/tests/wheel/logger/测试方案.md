# wheel/logger 测试方案

## 1. 被测目标

| 源文件 | 类型 | 测试文件 |
|--------|------|---------|
| `logger_models.h` — `LogConfig` | UT | `tests/wheel/logger/test_logger_ut.cpp` |
| `logger_factory.h` — `LoggerFactory` | UT | 同上 |
| `imp/spdlog_logger.h/.cpp` — `SpdlogLogger` | TA | `tests/wheel/logger/test_logger_ta.cpp` |

## 2. 测试点清单

### UT: LogConfig
- [x] struct 默认值 — level="info", log_file="data/lynne.log"
- [x] from_json 解析 — 部分字段、完整字段
- [x] LogLevel 枚举 — 值互异

### UT: Factory
- [x] create() — 返回 SpdlogLogger

### TA: SpdlogLogger 全链路
- [x] 生命周期 — start/stop/name/health
- [x] log 输出 — info/warn/error 级别
- [x] 文件写入验证
- [x] 临时目录隔离

## 3. 运行命令

```bash
dist/bin/test_logger_ut
dist/bin/test_logger_ta
```
