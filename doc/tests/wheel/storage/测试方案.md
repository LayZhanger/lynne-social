# wheel/storage 测试方案

## 1. 被测目标

| 源文件 | 类型 | 测试文件 |
|--------|------|---------|
| `storage_models.h` / `storage_factory.h` | UT | `tests/wheel/storage/test_storage_ut.cpp` |
| `imp/jsonl_storage.h/.cpp` — `JsonlStorage` | TA | `tests/wheel/storage/test_storage_ta.cpp` |

## 2. 测试点清单

### UT: 工厂 (3 例)
- [x] `create()` — 返回 JsonlStorage 默认 data_dir="data"
- [x] `create(StorageConfig)` — 传入 config 对象
- [x] `create(path)` — 传入字符串路径

### TA: JsonlStorage 全链路
- [x] `name` — "JsonlStorage"
- [x] `start()` — 创建 data 目录，health_check=True
- [x] `stop()` — 不抛异常
- [x] `save_items` + `load_items` — 往返一致
- [x] `load_items` 空目录 — 返回 `[]`
- [x] `load_items` 平台筛选
- [x] `save_items` 追加
- [x] report 往返 + 覆写
- [x] summary 往返
- [x] list_dates 倒序 + sessions 过滤
- [x] LLM 字段完整往返
- [x] 多日期隔离

## 3. 运行命令

```bash
dist/bin/test_storage_ut
dist/bin/test_storage_ta
```
