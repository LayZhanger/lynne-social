# core/browser 测试结果

## 1. 汇总

| 总数 | 通过 | 失败 | 覆盖率 |
|------|------|------|--------|
| 29 | 29 | 0 | 100% |

源文件覆盖率明细：

| 源文件 | 覆盖率 |
|--------|--------|
| `browser_models.py` | 100% |
| `browser_factory.py` | 100% |
| `browser_manager.py` | 100% |
| `imp/playwright_browser_manager.py` | 100% |
| `imp/playwright_context.py` | 100% |

测试耗时：~15s（TA 真实 Chromium headless 启动 ~1s/用例）

## 2. 用例明细

### UT: 数据模型 (4 例)

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_defaults` | `BrowserConfig()` | headless=False, slow_mo=500, viewport=1920×1080, locale=zh-CN, sessions_dir=data/sessions | ✅ |
| `test_custom_values` | 全部字段自定义 | 逐字段匹配 | ✅ |
| `test_equality` | 两个同值 | `a == b` | ✅ |
| `test_inequality` | 不同 headless | `a != b` | ✅ |

### UT: 工厂 (4 例)

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_create_default` | `create(None)` | instanceof PlaywrightBrowserManager, BrowserManager | ✅ |
| `test_create_with_config` | `create(BrowserConfig(headless=True, slow_mo=0))` | 实例 config 值匹配 | ✅ |
| `test_create_with_non_config_returns_default` | `create("not a config")` | 默认配置实例 | ✅ |
| `test_create_returns_browser_manager_interface` | `create(BrowserConfig())` | isinstance BrowserManager | ✅ |

### TA: 真实 Chromium headless 全链路 (17 例)

#### 生命周期

| 用例名 | 流程 | 验证点 | 状态 |
|--------|------|--------|------|
| `test_name` | 创建实例 | "PlaywrightBrowserManager" | ✅ |
| `test_health_check_before_start` | 未 start | health_check=False | ✅ |
| `test_full_lifecycle` | start → get_context → new_page → goto(data URL) → save_session → stop | 每步无异常, session 文件存在, stop 后 health_check=False | ✅ |
| `test_start_idempotent` | start × 2 → stop | 两次不抛异常, stop 后 health_check=False | ✅ |
| `test_stop_closes_all_contexts` | start → get_context × 2 → stop | _contexts 清空, health_check=False | ✅ |

#### Context 管理

| 用例名 | 流程 | 验证点 | 状态 |
|--------|------|--------|------|
| `test_get_context_creates_context` | started → get_context("twitter") | isinstance BrowserContext, raw() is not None | ✅ |
| `test_get_context_cached` | started → get_context × 2 | 引用相等 (is) | ✅ |
| `test_context_isolation` | started → get_context("twitter") → get_context("rednote") | 不相等, raw() 也不相等 | ✅ |
| `test_get_context_loads_existing_session` | A: start → get_context → add_cookies → save → stop
| | B: 新实例 start → get_context | Cookie 已持久化且可读取 | ✅ |

#### Session 持久化

| 用例名 | 流程 | 验证点 | 状态 |
|--------|------|--------|------|
| `test_save_session_persists_file` | started → get_context → save_session | JSON 文件存在, 含 cookies + origins | ✅ |
| `test_corrupted_session_graceful` | 损坏 JSON → start → get_context | 不抛异常, ctx instanceof BrowserContext | ✅ |

#### 页面操作

| 用例名 | 流程 | 验证点 | 状态 |
|--------|------|--------|------|
| `test_new_page_and_navigate` | started → get_context → new_page → goto → pw_page.title() | title == "Test" | ✅ |

#### Login Flow

| 用例名 | 流程 | 验证点 | 状态 |
|--------|------|--------|------|
| `test_login_flow_opens_page` | started → login_flow | _login_pending=True | ✅ |
| `test_set_login_complete_full_flow` | started → login_flow → set_login_complete | pending 清除, session 文件存在 | ✅ |
| `test_login_flow_reuses_context` | started → get_context → login_flow | _contexts 仍为 1 (复用) | ✅ |
| `test_stealth_applied_on_real_page` | started → login_flow(真实 data URL) | stealth 成功注入, pending=True | ✅ |
| `test_sequential_platform_operation` | start → 3 平台 get_context/new_page/goto/close → stop | 全部无异常 | ✅ |

### 错误路径 (4 例, 需要真实 Chromium 启动但错误在逻辑层)

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_get_context_not_started_raises` | 未 start → get_context | RuntimeError("not started") | ✅ |
| `test_save_session_no_context_raises` | started → save_session (无 context) | RuntimeError("no context") | ✅ |
| `test_set_login_complete_no_flow_raises` | started → set_login_complete | RuntimeError("no login flow active") | ✅ |
| `test_login_flow_stealth_missing_graceful` | mock ImportError → login_flow | 仍 goto, pending=True | ✅ |

---

## 3. 与 storage TA 对比

| 维度 | Storage TA | Browser TA |
|------|-----------|------------|
| 真实外部依赖 | 无（纯文件 IO） | Chromium headless 进程 |
| 隔离方式 | `tmp_path` 文件隔离 | `tmp_path` 文件隔离 + 独立 browser context |
| 单用例耗时 | <5ms | ~1s |
| mock 用例数 | 0 | 4 (错误路径) |
| 真实链路用例 | 19 | 17 |

---

*最后更新：2026-04-25*
