# Adapter 测试结果

**日期**：2026-04-25

**环境**：Python 3.12, Chromium headless, Playwright 1.58, pytest-asyncio

## 执行

```bash
pytest tests/core/adapters/ -v --cov=src/core/adapters --cov-report=term-missing
```

## 结果

| 文件 | 通过 | 失败 | 错误 | 时间 |
|------|------|------|------|------|
| test_adapter_models.py | 6 | 0 | 0 | 0.02s |
| test_adapter_factory.py | 4 | 0 | 0 | 0.01s |
| test_rednote_impl.py | 24 | 0 | 0 | 27s |

**总计**：34/34 通过

## 覆盖率

| 文件 | 语句 | 覆盖 | 未覆盖 |
|------|------|------|--------|
| `adapter_factory.py` | 20 | 100% | — |
| `adapter_models.py` | 11 | 100% | — |
| `base_adapter.py` | 5 | 100% | — |
| `imp/rednote_adapter.py` | 126 | 96% (121/126) | `_scrape_element` except 分支 (1 行), `_parse_int` 万/k ValueError 分支 (4 行) |

**模块总计**：168 语句，97% 覆盖

### 未覆盖分析

剩余 5 行均为防御性异常处理分支，触发条件为 DOM 元素在提取过程中被移除（需要竞态条件）或 `_parse_int` 的特定边界情况。实际生产环境中会自动触发，不影响正确性。

## 全量回归

```bash
pytest tests/ -v
```

138/138 通过，总覆盖率 97%。
