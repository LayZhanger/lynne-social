# common/models 测试结果

## 1. 汇总

| 总数 | 通过 | 失败 | 覆盖率 |
|------|------|------|--------|
| 13 | 13 | 0 | 100% |

## 2. 用例明细

| 用例名 | 输入 | 预期 | 状态 |
|--------|------|------|------|
| `test_create_with_defaults` | `UnifiedItem(platform="twitter", item_id="123")` | 必填字段正确，其余默认值 | ✅ |
| `test_create_full` | 全字段赋值 | 全字段值匹配 | ✅ |
| `test_to_dict` | `UnifiedItem` | dict 包含所有字段，空列表为 `[]` | ✅ |
| `test_to_json` | `UnifiedItem` | JSON 字符串，parse 后字段一致 | ✅ |
| `test_from_dict_full` | 含 LLM 字段的完整 dict | 逐字段还原，列表字段正确 | ✅ |
| `test_from_dict_empty` | `{}` | 空字符串/空列表/0，不抛异常 | ✅ |
| `test_from_dict_partial` | 仅 `platform` + `item_id` | 其余字段使用默认值 | ✅ |
| `test_from_dict_nulls` | `content_media=None, llm_tags=None, metrics=None` | 全部转为 `[]` / `{}` | ✅ |
| `test_roundtrip` | 创建 item → `to_dict()` → `from_dict()` | 全字段逐项一致 | ✅ |
| `test_defaults` (TaskResult) | `TaskResult(task_name="test")` | 数值 0，列表 `[]`，字符串 `""` | ✅ |
| `test_with_items` (TaskResult) | 全字段 + sample_items | 字段值匹配 | ✅ |
| `test_defaults` (RunStatus) | `RunStatus()` | `running=False, current_task=None, progress=""` | ✅ |
| `test_running` (RunStatus) | `RunStatus(running=True, current_task="AI动态", progress="...")` | 三字段值匹配 | ✅ |

## 3. 未覆盖说明

无。源文件 `src/common/models.py` 覆盖率 100%。
