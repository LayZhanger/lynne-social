# common/models 测试方案

## 1. 被测目标

| 源文件 | 类型 | 测试文件 |
|--------|------|---------|
| `src/common/models.h` — `UnifiedItem`, `RunStatus` | UT | `tests/common/test_models_ut.cpp` |
| `src/common/models.h/cpp` | TA | `tests/common/test_models_ta.cpp` |

## 2. 测试点清单

### UT: UnifiedItem
- [x] struct 默认值 — 只给 `platform` + `item_id`，其余取默认
- [x] 全字段赋值 — 包括 LLM 扩展字段
- [x] to_json / from_json — JSON 序列化往返
- [x] from_json 空 JSON — 所有字段回退默认
- [x] from_json null 值容错 — `content_media=null` → 空列表
- [x] 往返一致性

### UT: RunStatus
- [x] 默认值

### TA: 模型往返
- [x] BatchRoundTrip — 大量项目批量序列化/反序列化

## 3. 运行命令

```bash
./build.sh --test
# 或
dist/bin/test_models_ut
dist/bin/test_models_ta
```
